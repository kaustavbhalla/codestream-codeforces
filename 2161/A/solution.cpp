#include <bits/stdc++.h>
using namespace std;

int main() {
  ios::sync_with_stdio(false);
  cin.tie(NULL);

  int t;
  cin >> t;

  while (t--) {
    long long Ro, X, D, n;
    cin >> Ro >> X >> D >> n;

    long long R = Ro;

    string s;
    cin >> s;

    int count = 0;

    for (auto i : s) {
      if (i == '2') {
        if (R >= X) {
          continue;
        } else {
        }
      }
    }
  }
}
